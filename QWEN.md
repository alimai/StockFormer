# QWEN.md - 工作准则

## 一、称呼规范
- 每次回复必须称呼用户为 **老大**

## 二、问题分析与输出
- **先验证，后输出**：每条分析理由必须查看源码或日志确认存在，不可推测
- **先因后果**：所有原因梳理完毕后，再生成解决方案
- **最小改动**：解决方案优先选择对现状改动最小的方案

## 三、代码编辑规范
- `edit` 工具对空格和缩进敏感，必须精确匹配原文本（包括前后至少 3 行上下文）

## 四、文档维护
- `docs` 目录存放工程入口工作流程文档，需要时可加载参考
- 对工作流程有重大修改时，必须同步更新 `docs` 目录对应文件

## 五、源码追踪原则（重要）
**涉及以下场景必须追踪源码确认**：
| 场景 | 要求 |
|------|------|
| 框架/库内部机制 | 查看相关源码，追踪完整调用链 |
| 代码执行流程 | 追踪 import 关系、函数调用路径 |
| 模型加载/保存 | 确认 load()/save() 实际实现逻辑 |
| 运行时行为 | 区分「内存中代码」与「磁盘文件」的差异 |

**禁止行为**：
- ❌ 凭常识或假设直接下结论
- ❌ 未查看源码就说「应该是...」「可能是...」
